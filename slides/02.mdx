# To think in React:

- Break your UI into custom components.  
- Each component is responsible for displaying itself, 
    based on the _external_ and _internal_ data available.
- Build trees of these components for a full UI

